using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace CodeShopWarehouseBusinessTests
{
    [TestClass]
    public class MandatoryTests
    {
        [TestMethod]
        public void UnresolvedFillOrdersCanBeRetrieved()
        {
            bool assert = true;
            Assert.IsTrue(assert);
        }

        [TestMethod]
        public void UnresolvedFillOrderCanBeProcessed()
        {
            bool assert = true;
            Assert.IsTrue(assert);
        }

        [TestMethod]
        public void ProcessedFillOrderCannotBeModified()
        {
            bool assert = true;
            Assert.IsTrue(assert);
        }
    }
}
